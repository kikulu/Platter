# FIX_EXISTING_FEATURE_PROMPT.md — 既有功能修正提示詞模板

> 跟 `NEW_FEATURE_BUILD_PROMPT.md` 是一組的兩份文件，差別在於：
> - `NEW_FEATURE_BUILD_PROMPT.md`：**加東西**——會在 `BUILD_PLAN.md` 新增
>   一個 Stage，是「規格擴張」。
> - 本文件：**修東西**——目標是讓「已經寫在 `PROJECT_SPEC.md` / 已經被
>   `BUILD_PLAN.md` 某階段檢核表打勾」的功能，恢復成原本規格描述的行為，
>   是「修正回既有規格」，**原則上不應該新增 Stage、不應該改變
>   `PROJECT_SPEC.md` 的功能描述**（除非修正過程中發現規格本身寫得不清楚
>   或跟現實有落差，那才需要同步更新規格文字）。

---

## 什麼時候用這份、什麼時候用 `NEW_FEATURE_BUILD_PROMPT.md`

| 情境 | 用哪份 |
|---|---|
| 「複製提示詞按鈕沒反應」「刪除角色後某處還殘留舊 id」 | 本文件（修正） |
| 「我想要帳號清單可以拖曳排序」 | `NEW_FEATURE_BUILD_PROMPT.md`（新功能） |
| 「知識庫匯出的 Markdown 格式跟規格寫的不一樣」 | 本文件（修正，順便檢查是規格錯還是程式錯） |
| 「想加一個新平台」 | 介於兩者之間：如果只是照 `PLATFORM_URLS` 既有模式加設定值，
  比較接近修正／小幅擴充，可以直接用本文件；如果連帶要改 UI 結構，
  用新功能模板 |

---

## 修正時必須遵守的既有慣例（跟新功能模板相同，重複列一次方便單獨使用）

1. **核心原則不可違反**：不呼叫平台未公開 API、不繞過網站保護機制、
   不打包登入憑證進備份／匯出／文件庫檔案。修 bug 時特別注意——不要為了
   「順便讓擷取更穩定」就開始戳平台內部 API 或解析 Bearer Token。
2. **廣播 IPC 別漏掉**：如果修正牽涉到「A 視窗改了資料，B 視窗沒有跟著
   刷新」這類問題，检查對應動作後面有沒有呼叫
   `broadcastToAllWindows('accounts:changed' | 'knowledge:changed' |
   'documents:changed' | 'conversations:changed' | 'logs:changed' |
   'console:entry' | 'language:changed', ...)`。這正是 `CHANGELOG.md`
   `[1.6.1]` 那次修正的典型案例（`knowledge:group:*` 系列原本漏了廣播）。
3. **懸空引用清理**：如果修正牽涉到刪除某筆資料（角色/帳號/知識庫項目/
   套餐步驟），確認相關的反向引用（`roleIds`、`assigneeId`、
   `steps[].itemId`）有沒有一併清掉，不要留下壞掉的 id。
4. **不要順手重構跟這次修正無關的程式碼**。修 bug 的 diff 應該盡量小、
   聚焦在根因，方便回歸驗證；如果真的發現架構有明顯問題想順便改，
   先跟我確認，不要混在同一次修正裡。
5. **子視窗仍然要維持 `modal: false`**：修正視窗互動問題時，不要為了
   「解決點擊穿透」之類的症狀就加上 `modal: true`，這會鎖死主視窗、
   跟選取器工具衝突（見 `PROJECT_SPEC.md` 第 9 節開頭的說明）。

---

## 【提示詞本體】——複製這一段開新對話使用

```
你是要幫「Platter（AI Workspace Aggregator）」這個 Electron 專案修正一個
既有功能的問題，不是要加新功能。這個專案的規格記載在 PROJECT_SPEC.md，
建置歷程記載在 BUILD_PLAN.md（目前 16 個階段都已完成並勾選驗收，其中
Stage 9～16 還沒經過真正的 Electron 環境手動驗證，只做過語法檢查跟邏輯
層面的單元測試——如果你要修正的問題剛好落在這幾個階段的功能，這點特別
相關），實際版本變更記在 CHANGELOG.md。

請先讀這幾份文件，確認「規格上這個功能應該長什麼樣子」：
1. PROJECT_SPEC.md 裡跟這個功能相關的章節（自己判斷是第幾節，不確定就
   全文搜尋關鍵字）
2. BUILD_PLAN.md 裡對應階段的檢核表（確認這是不是原本就驗收過的行為）
3. CHANGELOG.md（確認這個功能最近有沒有被動過，是不是新的回歸問題）
4. 實際原始碼（main.js / preload.js / renderer/**）裡對應的實作

【問題描述】
<在這裡描述實際觀察到的錯誤行為：操作步驟、預期結果、實際結果、
是不是每次都會發生、有沒有 console/主程序錯誤訊息。>

【請你依序做這幾件事】
1. 先判斷這是「程式碼跟規格不一致」（程式碼有 bug，規格是對的，修程式碼）
   還是「規格本身寫得不清楚或已經過時」（程式碼行為其實合理，但
   PROJECT_SPEC.md 沒講清楚或講錯了）。兩種情況处理方式不一樣，先講清楚
   你判斷是哪一種、為什麼。
2. 找出根本原因，不要只是讓症狀消失（例如：不要用 try/catch 吞掉錯誤
   讓它看起來正常，要找到真正沒觸發的邏輯或漏掉的廣播事件）。
3. 修正時檢查有沒有連帶影響：
   - 這個修正會不會影響其他視窗的即時同步（廣播事件有沒有漏）？
   - 這個修正會不會影響匯入/匯出/備份的資料結構？
   - 這個修正會不會影響刪除時的連動清理？
4. 不要順手更動這次問題無關的程式碼風格或架構，除非你先跟我說明原因並
   取得同意。
5. 修正完成後：
   - 如果是純粹的程式碼 bug 修正：在 CHANGELOG.md 新增一筆 patch 版本
     （+0.0.1），簡短描述根因跟修正內容，格式比照既有條目（例如
     `[1.6.1]` 那筆）；同步更新 package.json 的 version。
   - 如果過程中發現 PROJECT_SPEC.md 或 BUILD_PLAN.md 描述跟實際行為
     對不上：同步修正對應文字，並在修正說明裡指出「這是文件澄清，不是
     行為變更」還是「這是行為變更，文件原本寫錯了」。
   - 不需要在 BUILD_PLAN.md 新增 Stage（除非這個修正大到變成事實上的
     新功能，那應該先跟我確認要不要改用新功能模板處理）。
6. 最後告訴我：怎麼手動驗證這個修正生效了（具體操作步驟），以及有沒有
   連帶驗證過原本 BUILD_PLAN.md 對應階段的檢核表項目沒有被這次修正
   弄壞（回歸測試）。

現在開始，如果問題描述不夠具體讓你無法定位根因，先問我要哪些資訊
（例如 console log、重現步驟），不要用猜的直接動手改。
```

---

## 附註：小到不需要走完整流程的情況

如果只是單一行的明顯筆誤（例如翻譯字串打錯字、CSS 顏色值寫錯），直接說
「幫我修正 XX 的這個小問題」即可，不用整段貼提示詞本體；上面的完整流程
主要是給「不確定根因在哪、可能牽動多個模組」的情況用的。
