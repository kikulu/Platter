# SPEC_ONLY_BUILD_PROMPT.md — 只附文件（不附程式碼）時的提示詞模板

> 跟 `NEW_FEATURE_BUILD_PROMPT.md` / `FIX_EXISTING_FEATURE_PROMPT.md` 是
> 同一組文件，差別在使用情境：那兩份預設「AI 有檔案系統工具，可以直接讀
> 專案原始碼、直接改檔案」（例如 Claude Code、Cursor、有檔案上傳+執行
> 環境的對話）。**這份文件是給「開新對話只附上幾份 .md 文件，不附上
> `main.js`/`preload.js`/`renderer/**` 原始碼」的情境**——常見於用純
> 聊天介面（沒有檔案系統工具）快速討論需求、規劃階段、或不想每次都上傳
> 一整包程式碼的時候。

---

## 為什麼要特別做這一份（不能直接沿用前兩份）

前兩份提示詞的邏輯是「先讀規格文件建立認知，再去讀實際原始碼確認」。
只附文件、不附程式碼的情境下，AI **沒有辦法確認 `PROJECT_SPEC.md` 描述
的行為跟實際程式碼是不是真的一致**——這不是理論疑慮：`CHANGELOG.md`
`[1.7.0]` 那筆紀錄本身就是一個實例，`README.md` 曾經寫著 `npm test`/
`npm run lint`/`npm run format` 都能用，但 `package.json` 其實完全沒有
對應的 scripts，直到 `1.7.0` 才真的補上。如果 AI 只看文件就直接假設
「規格寫的就是現況」，很容易在這種情境下做出跟實際程式碼對不上的建議。

所以這份提示詞的核心差異是：**把 `PROJECT_SPEC.md` 當成「意圖／宣稱的
行為」，不是「已驗證的事實」**，並且要求 AI 在不確定的地方主動跟你要
「現有程式碼的那一小段」，而不是憑空生成一份看起來合理、但實際上跟你
專案對不起來的程式碼。

---

## 該附上哪些文件（依情境挑選）

| 情境                             | 一定要附                                      | 建議附                                   | 不用附         |
| -------------------------------- | --------------------------------------------- | ---------------------------------------- | -------------- |
| 討論新功能規劃、還沒要真的寫程式 | `PROJECT_SPEC.md`                             | `BUILD_PLAN.md`、`ROADMAP.md`            | `CHANGELOG.md` |
| 新功能，要 AI 生出具體程式碼片段 | `PROJECT_SPEC.md`、`BUILD_PLAN.md`            | `CHANGELOG.md`（近期幾筆）、`ROADMAP.md` | —              |
| 修正某個功能的問題               | `PROJECT_SPEC.md`、`CHANGELOG.md`（近期幾筆） | `BUILD_PLAN.md` 對應階段                 | —              |

不用附 `CHECKLIST.md`——它已經標記棄用，內容跟現況不一致，附了只會誤導。
`README.en.md`/`README.ja.md` 通常也不需要，除非你要討論的剛好是多語系
文件本身的問題。

---

## 【提示詞本體】——複製這一段，貼在附件後面

```
你正在幫「Platter（AI Workspace Aggregator，Electron 專案）」討論
<新功能規劃 / 具體功能實作 / 問題修正>，但這次對話**只附上文件，沒有附上
實際原始碼**（main.js / preload.js / renderer/**）。請注意這件事會影響
你該怎麼回答：

【重要前提，請務必遵守】
1. 把附件裡的 PROJECT_SPEC.md 當成「這個專案宣稱／設計上應該有的行為」，
   不要當成「已經跟原始碼核對過、保證正確的事實」。文件跟實際程式碼
   有落差是真實發生過的事（見 CHANGELOG.md 裡 [1.7.0] 那筆——文件早就
   寫著某些指令可用，但程式碼其實沒做出來）。
2. 如果 CHANGELOG.md 裡有附最近幾筆紀錄，那些紀錄的時間點比
   PROJECT_SPEC.md 的敘述更接近「現況」；兩者描述衝突時，優先相信
   CHANGELOG 裡比較新的紀錄，並跟我說你發現了這個落差，不要自己選一邊
   就悶著頭做。
3. 你沒有這個專案的實際原始碼可以讀，所以：
   - 如果我要你**修正一個問題**，而你需要看到某個函式/某個檔案的現有
     內容才能判斷根因，**直接跟我要那一段程式碼**，不要用「規格上應該
     長這樣」去猜測現有實作、也不要假裝你看過。
   - 如果我要你**實作新功能**，你可以直接依照 PROJECT_SPEC.md 描述的
     架構慣例（資料層 loadX/saveX、IPC 用 broadcastToAllWindows 廣播、
     子視窗用 openChildWindow helper、i18n 用 data-i18n）生出完整的
     程式碼片段，但每個片段都要清楚標示：
     - 要放進哪個檔案（例如 `main.js`）
     - 放在什麼位置附近（例如「加在 registerIpcHandlers() 函式裡，
       `knowledge:list` handler 之後」），因為你沒辦法直接開檔案插入，
       只能靠文字描述位置讓我自己貼過去。
4. 不要假裝你「已經測試過」或「已經確認不會壞掉其他功能」——你沒有
   執行環境可以真的跑 `npm test`/`npm run lint`/`npm start`。你只能提供
   「應該怎麼手動驗證」的具體步驟，實際驗證要靠我自己做。

【核心原則，不能違反】
- 不呼叫平台未公開 API，對話擷取永遠只讀取當前畫面已渲染的 DOM。
- 不寫任何繞過網站保護機制（如 X-Frame-Options）的程式碼。
- 不把使用者登入憑證（cookie/localStorage/session token）放進任何備份/
  匯出/文件庫檔案。

【我要你做的事】
<在這裡描述：是新功能規劃、新功能實作、還是修正問題；具體需求或問題
現象是什麼；如果是修正問題，盡量描述操作步驟、預期結果、實際結果。>

【請你依序做這幾件事】
1. 先確認你手上的文件夠不夠回答這個需求；如果缺關鍵資訊（例如需要看
   某段程式碼、或需要知道某個資料結構的實際欄位），先列出你需要什麼，
   不要用猜的硬答。
2. 如果是新功能：
   a. 產出一段可以直接貼進 BUILD_PLAN.md 的新 Stage（目標/前置需求/
      交付項目/檢核表格式，比照現有階段的格式）。
   b. 產出具體程式碼片段，依上面第 3 點的方式標明檔案與插入位置。
   c. 產出一段可以貼進 PROJECT_SPEC.md 對應章節的文字，把新功能寫進
      「最終規格」。
   d. 產出一筆 CHANGELOG.md 條目草稿（版本號請先問我目前 package.json
      的版本，或請我提供，不要自己瞎猜目前版本號）。
3. 如果是修正問題：
   a. 先判斷這是「程式碼 bug」還是「規格描述本身就不清楚/過時」，講清楚
      你的判斷依據。
   b. 如果需要看現有程式碼才能定位根因，明確跟我要那一段（檔名+函式名/
      關鍵字都盡量講清楚，方便我快速找給你）。
   c. 拿到程式碼後才產出修正後的完整片段（不要只給 diff 描述，給我可以
      直接整段替換貼上的完整函式/區塊），並標明要貼回哪個檔案的哪個
      位置。
   d. 產出 CHANGELOG.md 條目草稿。
4. 最後列出我應該怎麼手動驗證這個改動（具體操作步驟 + 應該執行的指令，
   例如 `npm test`、`npm run lint`、`npm start` 後手動操作哪些流程），
   因為你沒辦法在這個對話裡實際跑起來驗證。

現在開始。如果我一開始給的描述不夠具體，先問我，不要用預設值硬猜。
```

---

## 附註：什麼時候該換成有檔案系統工具的對話

如果討論到後來發現改動範圍很大、牽動多個檔案、或需要反覆看程式碼才能
確認根因，這種「只貼文字、手動複製貼上」的模式會變得很沒效率——這時候
建議換成有檔案系統工具的環境（把整包專案上傳、或用 Claude Code /
Cursor 這類工具直接連專案資料夾），改用 `NEW_FEATURE_BUILD_PROMPT.md`
或 `FIX_EXISTING_FEATURE_PROMPT.md`，讓 AI 可以直接讀/改實際檔案，
不用你自己當「人肉剪貼簿」。
